# OSRS GE Terminal — backend dependencies.
# Pinned to the verified, running versions for reproducible builds — a rebuild
# months later resolves identically (a silent DuckDB minor bump could change file
# locking or the on-disk format). Bump these deliberately, not implicitly.
fastapi==0.137.1
uvicorn==0.49.0
httpx==0.28.1
duckdb==1.5.4
pandas==3.0.3
numpy==2.4.6
pydantic==2.13.4
truststore==0.10.4   # verify TLS against the OS trust store (corporate proxy CAs)

# Added in later passes for deeper statistics:
# scipy==1.14.x
# statsmodels==0.14.x
